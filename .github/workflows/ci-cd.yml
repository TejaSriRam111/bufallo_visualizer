name: CI/CD

on: [push]
  branches:
    - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        # DEPLOYMENT_URL: https://example.com
        # Add deployment command and arguments here

    env:
      DEPLOYMENT_URL: https://example.com